extends layout

block content

  script(src="/javascripts/voting.js")
  link(rel='stylesheet', href='/stylesheets/topic.css')
 
  #header
    h1(id="title")= title   
    h1(class='vote-header' id='remaining-votes') 5
    h1(class='vote-header') Remaining Votes:

  #content-header
    h1(id="topic-description") Who will be the next CEO?
   
  #content-block
    p(id='cand1') Sid :
      input(type='number' id='votes1' class='votes' min='0' max=5)
    p(id='cand2') Ethan :
      input(type='number' id='votes2' class='votes' min='0' max=5)
    p(id='cand2') Gennaro :
      input(type='number' id='votes2' class='votes' min='0' max=5)
    p
    input(type='submit' class='button' id='submit' value='Submit')
    